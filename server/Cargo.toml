[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
petgraph = "0.6"    # Graph data structure
tokio = { version = "1", features = ["full"] }
axum = { version = "0.7", features = ["ws"] }
serde_json = "1.0"